// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { InferenceEngine } from '../tryll/inference-engine.js';


/**
 * Every request carries a client-allocated monotonic request_id.
 * Configure session-level settings (must be sent before CreateAgentRequest).
 * Sets the inference engine that will be used for all agents on this session.
 */
export class ConfigureSessionRequest {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ConfigureSessionRequest {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsConfigureSessionRequest(bb:flatbuffers.ByteBuffer, obj?:ConfigureSessionRequest):ConfigureSessionRequest {
  return (obj || new ConfigureSessionRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsConfigureSessionRequest(bb:flatbuffers.ByteBuffer, obj?:ConfigureSessionRequest):ConfigureSessionRequest {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ConfigureSessionRequest()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

requestId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

inferenceEngine():InferenceEngine {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : InferenceEngine.Mock;
}

/**
 * When true, CreateAgent will automatically download any missing models
 * referenced by the graph instead of failing immediately.
 * Intended for development and prototyping — not for production use.
 * While downloading, DownloadProgress frames are emitted using the
 * CreateAgent request_id (informational only; the terminal frame is
 * CreateAgentResponse or ErrorResponse, not DownloadComplete).
 */
allowAutoModelDownloading():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

/**
 * Identifies the integration (e.g. "test-chat", "qa-and-eval", "<partner-game>").
 * Used for telemetry grouping.  Nullable — omitting it is valid for backward
 * compatibility; the server will default to "unknown" when telemetry is enabled.
 */
gameName():string|null
gameName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
gameName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Engine used to load STT (speech-to-text) models on this session.
 * Independent of inference_engine; STT/TTS/embedding lookups dispatch on this.
 */
sttEngine():InferenceEngine {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : InferenceEngine.Mock;
}

/**
 * Engine used to load TTS (text-to-speech) models on this session.
 */
ttsEngine():InferenceEngine {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : InferenceEngine.Mock;
}

/**
 * Engine used to load embedding models on this session.
 */
embeddingEngine():InferenceEngine {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : InferenceEngine.Mock;
}

/**
 * Optional per-session storage data folder. Relative storage paths in node
 * params (and voice-input hotwords) resolve against this folder. Empty means
 * not specified — the server falls back to its configured storage_root (which
 * itself defaults to the executable folder).
 */
storageDataFolder():string|null
storageDataFolder(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
storageDataFolder(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startConfigureSessionRequest(builder:flatbuffers.Builder) {
  builder.startObject(8);
}

static addRequestId(builder:flatbuffers.Builder, requestId:bigint) {
  builder.addFieldInt64(0, requestId, BigInt('0'));
}

static addInferenceEngine(builder:flatbuffers.Builder, inferenceEngine:InferenceEngine) {
  builder.addFieldInt8(1, inferenceEngine, InferenceEngine.Mock);
}

static addAllowAutoModelDownloading(builder:flatbuffers.Builder, allowAutoModelDownloading:boolean) {
  builder.addFieldInt8(2, +allowAutoModelDownloading, +false);
}

static addGameName(builder:flatbuffers.Builder, gameNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, gameNameOffset, 0);
}

static addSttEngine(builder:flatbuffers.Builder, sttEngine:InferenceEngine) {
  builder.addFieldInt8(4, sttEngine, InferenceEngine.Mock);
}

static addTtsEngine(builder:flatbuffers.Builder, ttsEngine:InferenceEngine) {
  builder.addFieldInt8(5, ttsEngine, InferenceEngine.Mock);
}

static addEmbeddingEngine(builder:flatbuffers.Builder, embeddingEngine:InferenceEngine) {
  builder.addFieldInt8(6, embeddingEngine, InferenceEngine.Mock);
}

static addStorageDataFolder(builder:flatbuffers.Builder, storageDataFolderOffset:flatbuffers.Offset) {
  builder.addFieldOffset(7, storageDataFolderOffset, 0);
}

static endConfigureSessionRequest(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createConfigureSessionRequest(builder:flatbuffers.Builder, requestId:bigint, inferenceEngine:InferenceEngine, allowAutoModelDownloading:boolean, gameNameOffset:flatbuffers.Offset, sttEngine:InferenceEngine, ttsEngine:InferenceEngine, embeddingEngine:InferenceEngine, storageDataFolderOffset:flatbuffers.Offset):flatbuffers.Offset {
  ConfigureSessionRequest.startConfigureSessionRequest(builder);
  ConfigureSessionRequest.addRequestId(builder, requestId);
  ConfigureSessionRequest.addInferenceEngine(builder, inferenceEngine);
  ConfigureSessionRequest.addAllowAutoModelDownloading(builder, allowAutoModelDownloading);
  ConfigureSessionRequest.addGameName(builder, gameNameOffset);
  ConfigureSessionRequest.addSttEngine(builder, sttEngine);
  ConfigureSessionRequest.addTtsEngine(builder, ttsEngine);
  ConfigureSessionRequest.addEmbeddingEngine(builder, embeddingEngine);
  ConfigureSessionRequest.addStorageDataFolder(builder, storageDataFolderOffset);
  return ConfigureSessionRequest.endConfigureSessionRequest(builder);
}
}
