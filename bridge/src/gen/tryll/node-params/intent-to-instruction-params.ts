// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

/**
 * Maps an intent label to an instruction text from a Map-kind string storage.
 * Resolution priority (factory): named string_storage → inline_keys + inline_strings
 * (Map-kind in-memory storage built from the equal-length pair).
 */
export class IntentToInstructionParams {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):IntentToInstructionParams {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsIntentToInstructionParams(bb:flatbuffers.ByteBuffer, obj?:IntentToInstructionParams):IntentToInstructionParams {
  return (obj || new IntentToInstructionParams()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsIntentToInstructionParams(bb:flatbuffers.ByteBuffer, obj?:IntentToInstructionParams):IntentToInstructionParams {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new IntentToInstructionParams()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * Named session Map-kind string storage.  Mutable — rebinds the storage.
 */
stringStorage():string|null
stringStorage(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
stringStorage(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Inline map keys (equal-length with inline_strings).  Structural because
 * they materialise the in-memory Map storage at construction.
 */
inlineKeys(index: number):string
inlineKeys(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
inlineKeys(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

inlineKeysLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Inline map values (equal-length with inline_keys).  Structural.
 */
inlineStrings(index: number):string
inlineStrings(index: number,optionalEncoding:flatbuffers.Encoding):string|Uint8Array
inlineStrings(index: number,optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__string(this.bb!.__vector(this.bb_pos + offset) + index * 4, optionalEncoding) : null;
}

inlineStringsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

/**
 * Name used as the Mustache variable suffix (instruction_<instruction_name>)
 * on the attached InstructionComponent.
 */
instructionName():string|null
instructionName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
instructionName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

/**
 * Default exit target after mapping an intent to an instruction.
 * Empty string = END.
 */
defaultExit():string|null
defaultExit(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
defaultExit(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startIntentToInstructionParams(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addStringStorage(builder:flatbuffers.Builder, stringStorageOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, stringStorageOffset, 0);
}

static addInlineKeys(builder:flatbuffers.Builder, inlineKeysOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, inlineKeysOffset, 0);
}

static createInlineKeysVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startInlineKeysVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addInlineStrings(builder:flatbuffers.Builder, inlineStringsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, inlineStringsOffset, 0);
}

static createInlineStringsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startInlineStringsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addInstructionName(builder:flatbuffers.Builder, instructionNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, instructionNameOffset, 0);
}

static addDefaultExit(builder:flatbuffers.Builder, defaultExitOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, defaultExitOffset, 0);
}

static endIntentToInstructionParams(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createIntentToInstructionParams(builder:flatbuffers.Builder, stringStorageOffset:flatbuffers.Offset, inlineKeysOffset:flatbuffers.Offset, inlineStringsOffset:flatbuffers.Offset, instructionNameOffset:flatbuffers.Offset, defaultExitOffset:flatbuffers.Offset):flatbuffers.Offset {
  IntentToInstructionParams.startIntentToInstructionParams(builder);
  IntentToInstructionParams.addStringStorage(builder, stringStorageOffset);
  IntentToInstructionParams.addInlineKeys(builder, inlineKeysOffset);
  IntentToInstructionParams.addInlineStrings(builder, inlineStringsOffset);
  IntentToInstructionParams.addInstructionName(builder, instructionNameOffset);
  IntentToInstructionParams.addDefaultExit(builder, defaultExitOffset);
  return IntentToInstructionParams.endIntentToInstructionParams(builder);
}
}
