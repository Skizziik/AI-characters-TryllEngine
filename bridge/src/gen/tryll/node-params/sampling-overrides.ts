// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

/**
 * Sparse sampling override block.
 * Null on any field means "inherit the selected model's default".
 * No per-field (on_change) attributes: the outer sub-table field carries one
 * callback that re-resolves the whole block and commits derived state.
 */
export class SamplingOverrides {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SamplingOverrides {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSamplingOverrides(bb:flatbuffers.ByteBuffer, obj?:SamplingOverrides):SamplingOverrides {
  return (obj || new SamplingOverrides()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSamplingOverrides(bb:flatbuffers.ByteBuffer, obj?:SamplingOverrides):SamplingOverrides {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SamplingOverrides()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * 0.0 = fully deterministic, 2.0 = highly random.  Null = inherit.
 */
temperature():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : null;
}

/**
 * Nucleus sampling cutoff.  Null = inherit model default.
 */
topP():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : null;
}

/**
 * Top-k sampling limit.  Null = inherit model default.
 */
topK():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : null;
}

/**
 * Minimum token probability (min-p sampling).  Null = inherit model default.
 */
minP():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : null;
}

/**
 * Maximum new tokens to generate.  Null = inherit model default.
 */
maxTokens():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : null;
}

/**
 * RNG seed for reproducible sampling.  Null = inherit model default.
 */
seed():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : null;
}

/**
 * Penalise repeated tokens (llama.cpp repeat_penalty).  Null = inherit.
 */
repeatPenalty():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : null;
}

/**
 * OpenAI-style presence penalty.  Null = inherit model default.
 */
presencePenalty():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : null;
}

/**
 * OpenAI-style frequency penalty.  Null = inherit model default.
 */
frequencyPenalty():number|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.readFloat32(this.bb_pos + offset) : null;
}

static startSamplingOverrides(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addTemperature(builder:flatbuffers.Builder, temperature:number) {
  builder.addFieldFloat32(0, temperature, null);
}

static addTopP(builder:flatbuffers.Builder, topP:number) {
  builder.addFieldFloat32(1, topP, null);
}

static addTopK(builder:flatbuffers.Builder, topK:number) {
  builder.addFieldInt32(2, topK, null);
}

static addMinP(builder:flatbuffers.Builder, minP:number) {
  builder.addFieldFloat32(3, minP, null);
}

static addMaxTokens(builder:flatbuffers.Builder, maxTokens:number) {
  builder.addFieldInt32(4, maxTokens, null);
}

static addSeed(builder:flatbuffers.Builder, seed:number) {
  builder.addFieldInt32(5, seed, null);
}

static addRepeatPenalty(builder:flatbuffers.Builder, repeatPenalty:number) {
  builder.addFieldFloat32(6, repeatPenalty, null);
}

static addPresencePenalty(builder:flatbuffers.Builder, presencePenalty:number) {
  builder.addFieldFloat32(7, presencePenalty, null);
}

static addFrequencyPenalty(builder:flatbuffers.Builder, frequencyPenalty:number) {
  builder.addFieldFloat32(8, frequencyPenalty, null);
}

static endSamplingOverrides(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSamplingOverrides(builder:flatbuffers.Builder, temperature:number|null, topP:number|null, topK:number|null, minP:number|null, maxTokens:number|null, seed:number|null, repeatPenalty:number|null, presencePenalty:number|null, frequencyPenalty:number|null):flatbuffers.Offset {
  SamplingOverrides.startSamplingOverrides(builder);
  if (temperature !== null)
    SamplingOverrides.addTemperature(builder, temperature);
  if (topP !== null)
    SamplingOverrides.addTopP(builder, topP);
  if (topK !== null)
    SamplingOverrides.addTopK(builder, topK);
  if (minP !== null)
    SamplingOverrides.addMinP(builder, minP);
  if (maxTokens !== null)
    SamplingOverrides.addMaxTokens(builder, maxTokens);
  if (seed !== null)
    SamplingOverrides.addSeed(builder, seed);
  if (repeatPenalty !== null)
    SamplingOverrides.addRepeatPenalty(builder, repeatPenalty);
  if (presencePenalty !== null)
    SamplingOverrides.addPresencePenalty(builder, presencePenalty);
  if (frequencyPenalty !== null)
    SamplingOverrides.addFrequencyPenalty(builder, frequencyPenalty);
  return SamplingOverrides.endSamplingOverrides(builder);
}
}
