// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

/**
 * Confirms a request that has no dedicated response table (DestroyAgent,
 * ChangeAgentParam, UnloadModel, Destroy*Storage, VoiceInput control ops, …).
 * agent_id (index 1) carries the owning agent for agent-scoped acks
 * (e.g. ChangeAgentParam); zero for non-agent-scoped acks.
 */
export class Ack {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Ack {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsAck(bb:flatbuffers.ByteBuffer, obj?:Ack):Ack {
  return (obj || new Ack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsAck(bb:flatbuffers.ByteBuffer, obj?:Ack):Ack {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Ack()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

requestId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

agentId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readUint64(this.bb_pos + offset) : BigInt('0');
}

static startAck(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addRequestId(builder:flatbuffers.Builder, requestId:bigint) {
  builder.addFieldInt64(0, requestId, BigInt('0'));
}

static addAgentId(builder:flatbuffers.Builder, agentId:bigint) {
  builder.addFieldInt64(1, agentId, BigInt('0'));
}

static endAck(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createAck(builder:flatbuffers.Builder, requestId:bigint, agentId:bigint):flatbuffers.Offset {
  Ack.startAck(builder);
  Ack.addRequestId(builder, requestId);
  Ack.addAgentId(builder, agentId);
  return Ack.endAck(builder);
}
}
